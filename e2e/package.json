{
  "name": "sdip-e2e",
  "private": true,
  "description": "End-to-end tests that drive the docker-compose deployment with Playwright",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "@types/node": "^22.16.5"
  }
}
